# Railway Modelling Directory Cloudflare Worker

This is the code behind the Cloudflare Email -> Webhook

It's deployed to Cloudflare as a worker and then configured as an Email -> Worker route.

Requires `WEBHOOK_URL` to be configured.